import { black, white } from 'typestyle/csx';

const baseBgColor = black;
const baseFgColor = white;

const baseFontSize = 10.5;

export const colors = {
    baseBgColor: baseBgColor,    
    baseFgColor: baseFgColor
};

export const fonts = {
    baseFont: 'Montserrat',
    baseFontSize: baseFontSize + 'pt'
    
};
